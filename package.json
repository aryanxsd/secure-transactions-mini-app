{
  "name": "secure-transactions",
  "private": true,
  "packageManager": "pnpm@10.29.2",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.8.4",
    "typescript": "^5.3.3"
  }
}
